name: teologist

services:
  app:
    build:
      context: ..
      dockerfile: docker/Dockerfile.prod
      target: app
    restart: unless-stopped
    environment:
      APP_ENV: production
      APP_DEBUG: "false"
      APP_URL: https://${APP_DOMAIN}
      APP_KEY: ${APP_KEY}
      DB_CONNECTION: pgsql
      DB_HOST: database
      DB_PORT: 5432
      DB_DATABASE: ${POSTGRES_DB:-teologist}
      DB_USERNAME: ${POSTGRES_USER:-teologist}
      DB_PASSWORD: ${POSTGRES_PASSWORD}
    depends_on:
      database:
        condition: service_healthy
    networks:
      - internal

  web:
    build:
      context: ..
      dockerfile: docker/Dockerfile.prod
      target: web
    restart: unless-stopped
    depends_on:
      - app
    networks:
      - internal
      - proxy
    labels:
      - traefik.enable=true
      - traefik.docker.network=proxy
      - traefik.http.routers.teologist.rule=Host(`${APP_DOMAIN}`)
      - traefik.http.routers.teologist.entrypoints=websecure
      - traefik.http.routers.teologist.tls=true
      - traefik.http.routers.teologist.tls.certresolver=letsencrypt
      - traefik.http.services.teologist.loadbalancer.server.port=80

  database:
    image: postgres:18-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-teologist}
      POSTGRES_USER: ${POSTGRES_USER:-teologist}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - internal

volumes:
  postgres_data:

networks:
  internal:
  proxy:
    external: true
