<header class="w-full bg-gradient-to-b from-black/5 to-transparent text-white">
    <div class="container mx-auto px-4 py-4 flex items-center justify-between">
        <a href="{{ route('home') }}" class="font-bold text-xl">Phrónesis</a>
        <nav class="hidden md:flex items-center gap-6">
            <a href="{{ route('blog') }}" class="hover:font-semibold">Blog</a>
            <a href="{{ route('forum') }}" class="hover:font-semibold">Fórum</a>
            <a href="{{ route('noticias') }}" class="hover:font-semibold">Notícias</a>
        </nav>
    </div>
</header>
